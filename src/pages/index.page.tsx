import { ThemeChanger } from "src/components/ThemeChanger";
import { Layout } from "src/layout";

const Home = () => {
  return (
    <Layout>
      <ThemeChanger />
    </Layout>
  );
};

export default Home;
